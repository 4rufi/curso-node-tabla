#Notes:
project to create multiplication tables

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     base for multiplication table               [number] [required]
  -l, --limit    limit for multiplication table              [number] [required]
  -s, --show     show table in console                [boolean] [default: false]
```
